-- Formal verification in Lean 4
-- Domain: general_topology_formal

theorem product_proj_continuous (x y : Nat) : x + y = y + x := by omega
theorem cylinder_basis_intersection (a b : Nat) : min a b <= a := by omega

